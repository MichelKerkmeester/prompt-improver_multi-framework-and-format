I need two things from you before I can improve this prompt.

**1. Which mode did you mean?**
You used both `$short` and `$deep`, and they conflict.
- **`$short`**: quick, lean enhancement with smart defaults.
- **`$deep`**: full DEPTH rigour, all 5 perspectives, and framework selection matched to the prompt's complexity.

**2. What is the prompt to improve?**
"My prompt" didn't come with the text. Please paste your current weekly meal-plan generator prompt. If you don't have one yet, describe what the AI should do.

The optional details below help, and I'll use smart defaults and flag them as `[Assumes: ...]` if you skip them:
- The target model or tool, such as ChatGPT, Claude or an API.
- Any inputs the plan should take, such as dietary needs, budget, calorie or macro targets, servings, cooking time or pantry items.
- The output you want, such as a table, a shopping list or recipes.
- The output format for the improved prompt: Markdown, JSON or YAML. The default is Markdown.

Reply with your mode choice (`$short` or `$deep`) and your prompt.