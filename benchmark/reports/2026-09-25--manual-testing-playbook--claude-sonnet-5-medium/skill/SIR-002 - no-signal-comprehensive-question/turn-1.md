I can help, but I'm set up to improve prompts, not to write or edit the draft itself. If your draft is a prompt for an AI, I can turn it into a clearer, more structured one.

Could you answer these in one reply?

1. **The draft:** paste the text of the prompt you've been stuck on.
2. **What it's for:** what should the AI produce with it (for example an email, a strategy doc, code, an image or a video), and which tool or model will run it?
3. **Mode or format (optional):** do you want a quick cleanup (`$short`), a deeper rewrite with framework scoring (`$deep`), or a specific output format (`$json`, `$yaml`, `$markdown`)? If you don't say, I'll pick based on the draft.

Once I have that, I'll save the improved prompt to `export/` and send you the file path with a short summary.