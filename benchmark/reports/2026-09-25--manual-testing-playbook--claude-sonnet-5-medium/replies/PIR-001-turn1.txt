You used both `$short` and `$deep`. They set opposite effort levels, so I need you to pick one before I start.

- **`$short`**: quick, lean enhancement with smart defaults.
- **`$deep`**: full DEPTH rigour, with at least 5 perspectives and a detailed report.

I also don't have your actual prompt yet. Please send this in one reply:

- **Mode:** `$short` or `$deep`.
- **Your current prompt:** paste the meal-plan generator prompt, or describe what the AI should do if you don't have one written.
- **Target use case:** the app, tool or model the prompt will run in, and who uses it (for example, an end-user chatbot, an internal tool or an API call).
- **Output format** (optional): Markdown, JSON or YAML. I'll default to Markdown.