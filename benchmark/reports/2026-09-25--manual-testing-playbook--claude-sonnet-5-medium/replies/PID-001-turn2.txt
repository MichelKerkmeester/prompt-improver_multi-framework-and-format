```markdown
Mode: $text | Complexity: 2 | Framework: RCAF

**Role:** Friendly member-experience copywriter for [Gym Name], writing the first onboarding message that new members receive after signing up in the [App Name] app.

**Context:** A new member has just joined through the app. Early visits build the habit that keeps members coming back. This email's job is to get them back into the app to start their free trial week and book a first class. The email must mention two things: the free trial week ([how it starts, e.g. "starts today" or "activate in the app"]) and the class schedule ([where to find it, e.g. "in the app's Classes tab" or [Schedule Link]]). [Assumes: the member signed up in the app and the email is sent right after sign-up; the trial week is included with membership; the schedule is viewable in the app or through a link; the email should drive the member to open the app.]

**Action:** Write one welcome email containing:
- A subject line
- A greeting using [First Name]
- A warm welcome to [Gym Name]
- One sentence about the free trial week
- One sentence saying where to find the class schedule in [App Name]
- One clear call to action to open the app and book a first class, linking to [App Link]
- A sign-off from [Sender Name]

**Format:** Plain-text email in a friendly, conversational tone. The whole email must be under 120 words, counting the greeting and sign-off. The subject line is separate and should be short. Use no headings or bullet points. Return only the email.

---
Attestation: docs consulted = DEPTH Thinking Framework, Interactive Mode, Patterns and Evaluation, Framework Pattern Library, Format Guide Markdown | assumptions = email is sent after in-app sign-up; trial week included with membership; schedule viewable in the app or via link; app name, gym name, sender and links left as placeholders | format = Markdown | execution = did not occur | save = did not occur
```

The block is inline in chat again, since I still have no artifact tool.

**Export-equivalent path:** `export/[###] - enhanced-gym-app-onboarding-email.md`. Replace `[###]` yourself.

**Score and gate:** CLEAR 45/50 (C 9, L 9, E 13, A 9, R 5). Three perspectives were applied. The gate passed.

**What changed:**
- **App context:** The prompt now frames the email as the first onboarding touchpoint after sign-up in your app. The call to action sends members into the app to start the trial and book a class.
- **Placeholders:** The app name and links are placeholders, so you can reuse it.

**Questions for you:**
- Do you mean an email, or an in-app message such as a welcome screen or push notification? I kept it as an email. If it's an in-app message, say so and I'll rework the format and word limit.
- What is the trial week's exact activation step, for example automatic or a button in the app? I didn't invent one, and the prompt flags it as an assumption.