# PCR-001 -- Image mode VISUAL gate and follow-up in the Project (project)

Runtime: project | Model: claude-sonnet-5 | Effort: medium
Harness: run_packaging.sh "Prompt Improver" project --noweb --session (single turn, Turn 1 delivered directly)
Turn count: 1 (Turn 1 delivered directly rather than asking a question, so the scenario's conditional Turn 2 was not triggered)

## Turn 1 prompt

$image Build me a Midjourney prompt for a misty forest cabin at dawn with cinematic light.

## Turn 1 reply (raw chat capture, DELIVERABLE tag added at capture time around the rendered block)

## Deliverable

<DELIVERABLE>
**Mode:** $image | **Platform:** Midjourney v6.1 | **Complexity:** 3 | **Framework:** FRAME | **Score:** VISUAL 54/60

```
Rustic log cabin nestled in a misty pine forest at dawn, warm lantern glow
in one window, thin smoke rising from the stone chimney. Volumetric fog
drifts low between towering pines as golden-pink sunrise light breaks
through the canopy in cinematic god rays. Dew-covered moss and fallen
needles in the foreground, wide shot, rule-of-thirds composition,
cinematic film still --ar 16:9 --s 400 --style raw --no people, text
```

---
Attestation: docs consulted = Image Mode, Image Mode Library, Patterns and Evaluation | assumptions = single-cabin subject, no human figures, standard 16:9 cinematic framing | format = Markdown | execution = did not occur | save = did not occur
</DELIVERABLE>

**Export-equivalent path:** `export/001 - enhanced-misty-forest-cabin-dawn.md`

**Score and gate status:** VISUAL 54/60 | Vivid 14/15, Intentional 9/10, Styled 8/10, Unambiguous 9/10, Atmospheric 10/10, Layered 4/5 | Gate passed (48+ threshold)

**Summary:** Applied the FRAME framework — a specific cabin subject with sensory detail (Focus), cinematic film-still rendering (Rendering), dawn volumetric fog and god-ray lighting (Atmosphere), Midjourney's `--ar`/`--s`/`--style raw` modifiers, and a partial `--no` exclusion for people/text since Midjourney only partially supports negatives. Word count and syntax fit Midjourney's 20-60-word + params sweet spot.

**Share Your Generated Image for Refinement**
Try this prompt in Midjourney and share the result — I can help refine composition, atmosphere, or specific elements (e.g., mist density, cabin detail, light color) based on what it actually renders.

## Grading notes

- VISUAL scored (not CLEAR or EVOKE), gate passed at 54/60 (above the 48/60 threshold), Midjourney platform named throughout, attestation complete with both required fields, no claimed save, and the mandatory share-back invitation is present. The block renders before any commentary (only a bare "## Deliverable" heading precedes it, labeling the block rather than adding separate commentary), matching the non-identity Artifact-first shape.
- Tool-detection caveat for the header line: the model bolds each label individually (`**Mode:**`, `**Complexity:**`, etc.) rather than opening the line with a plain `Mode:` token. `deliverable_lint.py`'s `MODE_LINE` regex anchors on a line that starts with `Mode:` after optional whitespace, so a bolded label (`**Mode:**`) will not match it even though the header is substantively complete (all four required fields plus a platform field are present, matching PID-001's own bolded-line pattern). This is a linter-detection limitation, not a defect in the reply, and it is called out here ahead of running the automated checks so the finding is not misread as a missing header.
- Verdict: PASS
