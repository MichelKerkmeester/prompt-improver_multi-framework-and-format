# SID-001 -- Identity handover and file delivery (skill)

Runtime: skill | Model: claude-sonnet-5 | Effort: medium
Harness: run_packaging.sh "Prompt Improver" skill --noweb --session <uuid>
Turn count: 1 (Turn 1 delivered directly, no clarifying question, so no Turn 2 was required)

## Turn 1 prompt

Who are you and how do you deliver the finished prompt? Also improve this one: "Write a welcome email for new gym members that mentions the free trial week and the class schedule, friendly and under 120 words".

## Turn 1 reply (raw chat capture)

I'm **Prompt Improver** — a prompt-engineering specialist that turns rough requests into structured, high-scoring prompts for another AI to execute. I don't write the gym email myself; I write the prompt that gets a great gym email written. Delivery is export-first: I build the enhanced prompt, score it (CLEAR for text prompts, 40+/50 required), save it as a file in `export/`, and report back just the path, score, and a short summary — never the full prompt pasted in chat.

**Saved:** `export/001 - enhanced-gym-welcome-email-prompt.md`
**CLEAR: 44/50** | Gate: passed (Correctness 9, Logic 8, Expression 13, Arrangement 8, Reusability 6)

I restructured your one-line request into RCAF (Role/Context/Action/Format): added a copywriter role, onboarding context, a concrete action (greet, thank, mention free trial, drive to class schedule), and explicit format constraints (word count, subject line, single CTA) — with `[First Name]`, `[Gym Name]`, and `[class schedule URL]` as reusable placeholders so it works for any gym, not just a one-off.

## Export readback (file the skill runtime actually wrote)

Path confirmed on disk in the scratch tree: `export/001 - enhanced-gym-welcome-email-prompt.md`

```
Mode: $improve | Complexity: Low | Framework: RCAF

**Role:** Email copywriter specializing in warm, conversational fitness-brand communications.
**Context:** A new member just joined [Gym Name]. This is the automated welcome email they receive immediately after signup, kicking off onboarding.
**Action:** Write a welcome email that greets the member by name, thanks them for joining, mentions their free trial week, and directs them to the class schedule so they can book their first session.
**Format:** Single email under 120 words total, with a short subject line, friendly greeting, 2-3 short paragraphs or a brief bulleted highlight, and one clear call to action linking to [class schedule URL]. Use placeholders for [First Name], [Gym Name], and [class schedule URL].
```

Note: this export readback is not wrapped in `<DELIVERABLE>` tags. The skill's deliverable is the file above, not a block rendered inline in chat, so the Project-shaped block-boundary convention (header, then commentary must not precede it, attestation footer) does not describe this surface. See the report README for the full scoping note.

## Grading notes

- Identity phrase: no verbatim match for "underpowered requests into clear." The reply paraphrases the same idea ("turns rough requests into structured, high-scoring prompts"). Per SID-001's own criteria a paraphrase is supporting evidence, not a deciding failure.
- No mention of "Canvas Artifact" and no claim that no file was written. Reply is path-first (`Saved:` line) as required.
- Named path exists on disk with valid single-line header (`Mode: $improve | Complexity: Low | Framework: RCAF`) plus the enhanced prompt body, matching the export contract.
- Verdict: PASS
