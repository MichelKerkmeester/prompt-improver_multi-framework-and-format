# SIR-002 -- No-signal request gets one comprehensive question (skill)

Runtime: skill | Model: claude-sonnet-5 | Effort: medium
Harness: run_packaging.sh "Prompt Improver" skill --noweb --session/--resume
Turn count: 2 (mandatory per scenario contract)

## Turn 1 prompt

Hey, I could use a hand with a draft I have been stuck on all week.

## Turn 1 reply (raw chat capture)

I'd love to help you get unstuck! To point you in the right direction, could you share a few things:

1. **What's the draft?** (paste the current text, or describe the task if there's nothing written yet)
2. **What's it for?** (e.g., a sales email, a blog post, an internal memo, a product description, social copy, etc.)
3. **What's the sticking point?** (tone feels off, too vague, structure isn't landing, doesn't sound like you, etc.)
4. **Any preferred output format** — plain text/Markdown is the default, but let me know if you need JSON or YAML for a tool/pipeline.

Once I have those, I'll turn it into a clear, well-structured prompt you can hand to an AI (or use yourself) to actually produce the draft — and I'll save the finished prompt to a file so you have it on hand.

State check: `export/` confirmed empty in the scratch tree immediately after this turn.

## Turn 2 prompt

It is a prompt for a support chatbot. Tighten it up: "Answer the customer nicely and fix their problem".

## Turn 2 reply (raw chat capture)

Saved: `export/001 - enhanced-support-chatbot-prompt.md`

**CLEAR 44/50 | Gate: passed**

I used RCAF (fits a low-complexity, single-outcome chatbot instruction) and turned the two vague verbs — "answer nicely" and "fix their problem" — into a concrete Role/Context/Action/Format spec: empathetic acknowledgment → root-cause diagnosis → step-by-step resolution or a clear escalation path, with plain-language tone and a mandatory close-out confirmation. Bracketed placeholders (`[product/company name]`, `[support channel]`, `[escalation policy]`) keep it reusable across your actual stack — swap those in and it's ready to drop into the bot's system prompt.

Want me to tighten it further into a single-line version, or expand the escalation branch (e.g., billing vs. technical routing)?

## Export readback (file the skill runtime actually wrote on Turn 2)

Path confirmed on disk: `export/001 - enhanced-support-chatbot-prompt.md` (file exists, excerpt not re-quoted here, header/body shape matches the same single-line-header contract shown in SID-001 and SIR-001).

Not wrapped in `<DELIVERABLE>` tags for the same reason as SID-001 and SIR-001: this is a real file on disk, not an inline rendered block.

## Grading notes

- Turn 1 asked exactly one comprehensive question (draft, use case, sticking point, format all bundled in one turn) and waited, with no file written and no self-answering.
- Turn 2 used only Turn-2-supplied facts (support chatbot, "answer nicely," "fix their problem") to drive the export, no invented requirements, saved a real file, passed CLEAR (44/50), replied path-first.
- Verdict: PASS (both turns required and both correct)
