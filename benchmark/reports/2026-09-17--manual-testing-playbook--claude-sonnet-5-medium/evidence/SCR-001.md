# SCR-001 -- Image mode VISUAL gate and follow-up (skill)

Runtime: skill | Model: claude-sonnet-5 | Effort: medium
Harness: run_packaging.sh "Prompt Improver" skill --noweb --session (single turn, Turn 1 delivered directly)
Turn count: 1 (Turn 1 delivered directly rather than asking a question, so the scenario's conditional Turn 2 was not triggered)

Harness note: the first attempt at this scenario hit a transient `run_packaging.sh: line 150: syntax error near unexpected token 'then'` because the shared script was being edited by another lane mid-invocation (it grew from 151 to 167 lines between this run and the prior pair, adding a `--seed` feature). `bash -n` on the script confirmed valid syntax moments later. The half-registered session id from that aborted attempt came back "already in use" on a same-id retry, so a fresh session id was generated and Turn 1 was run again cleanly. Recorded here for transparency. It is a shared-harness contention event, not a Prompt Improver behavior.

## Turn 1 prompt

$image Build me a Midjourney prompt for a misty forest cabin at dawn with cinematic light.

## Turn 1 reply (raw chat capture)

Saved: `export/001 - enhanced-midjourney-forest-cabin-prompt.md`

VISUAL 53/60 | Gate: passed (Vivid 14, Intentional 9, Styled 9, Unambiguous 9, Atmospheric 9, Layered 5)

Built with FRAME for Midjourney v6.1: a specific cabin/forest subject with clear composition (F), cinematic film-still rendering with lens and film-stock cues (R), layered dawn mist/light atmosphere (A), `--ar 16:9 --s 450 --style raw` plus a light `--no` exclusion list for Midjourney's partial negative support (M/E). Share the generated image if you want another refinement pass.

## Export readback (file the skill runtime actually wrote)

```
$image | Complexity: Standard | Framework: FRAME | Platform: Midjourney v6.1

A weathered log cabin with a single glowing amber window, nestled at the edge of a dense pine and birch forest, wisps of low-hanging mist curling around the tree trunks and rooftop, wide shot at eye level with the cabin placed on the lower-right third and a leading path of fog drawing the eye toward it, soft pre-dawn light breaking through the canopy in pale gold and cool blue shafts, volumetric god rays filtering through the mist, dew-covered pine needles and moss-covered stones in the foreground, layered depth with foreground ferns, midground cabin, and background trees fading into haze, cinematic film-still photography, shot on anamorphic lens, Kodak Portra 400 color grade, tranquil and quietly hopeful mood --ar 16:9 --s 450 --style raw --no text, watermark, people, cars
```

Not wrapped in `<DELIVERABLE>` tags for the same reason as the other skill captures: this is a real file on disk, not an inline rendered block.

## Grading notes

- VISUAL scored (not CLEAR or EVOKE), gate passed at 53/60 (above the 48/60 threshold), Midjourney platform retained throughout (both in chat and in the exported prompt's `--ar`/`--s`/`--style raw` syntax), and the mandatory share-back invitation is present ("Share the generated image if you want another refinement pass").
- Advisory, not blocking: the exported file's header line reads `$image | Complexity: Standard | Framework: FRAME | Platform: Midjourney v6.1`, dropping the literal `Mode:` label that every other skill export in this run used (SID-001, SIR-001, STX-001, SFM-001 all open with `Mode: $...`). SCR-001's own pass/fail criteria does not name a header-label requirement the way SFM-001 does for the JSON scenario, so this is recorded as a formatting inconsistency worth a look, not a scenario failure. It also adds a `Platform:` field beyond the base template, matching the Project twin's own header shape.
- Verdict: PASS
