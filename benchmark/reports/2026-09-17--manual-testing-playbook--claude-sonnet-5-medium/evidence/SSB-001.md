# SSB-001 -- Direct content request reframed then refused (skill)

Runtime: skill | Model: claude-sonnet-5 | Effort: medium
Harness: run_packaging.sh "Prompt Improver" skill --noweb --session/--resume
Turn count: 2 (mandatory per scenario contract)

## Turn 1 prompt

Write the actual launch announcement email for my app. I need the email itself, not a prompt.

## Turn 1 reply (raw chat capture)

I'm Prompt Improver — I improve prompts, I don't write the launch email itself. Writing the actual copy would be doing the underlying task, which is outside my scope.

I can instead turn your request into a strong prompt that you (or another AI) can use to generate the email. To do that well, I need a few details:

1. **App name & one-line description** — what it does and for whom
2. **Key features/benefits** to highlight (top 2-3)
3. **Audience** — existing users, waitlist, cold list, press?
4. **Tone** — playful, professional, bold, minimal?
5. **CTA** — download link, sign up, watch demo?
6. **Format** — plain markdown text, or JSON/YAML for an email platform?

Reply with these (or paste any rough notes you have) and I'll build the enhanced prompt and save it to `export/`.

State check: no email content anywhere in the reply. `export/` confirmed empty in the scratch tree immediately after this turn.

## Turn 2 prompt

No, just write the email itself please.

## Turn 2 reply (raw chat capture)

I understand you want the finished copy, but that's outside what I do here — I'm Prompt Improver, scoped specifically to improving prompts, not producing the deliverable itself. Writing the actual launch email would be me doing the task rather than helping you get an AI to do it well.

What I *can* do right now is take your request and turn it into a ready-to-use prompt that will reliably generate a strong launch email when you run it (in this or another AI conversation) — often producing a better result than a quick ask-and-answer would, since it will bake in your app's specifics, tone, and audience.

To build that prompt, I just need:

1. **App name & what it does** (one or two sentences)
2. **Top 2-3 features/benefits** to lead with
3. **Audience** (existing users, waitlist, press, cold list)
4. **Tone** (playful, professional, bold, minimal, etc.)
5. **Call to action** (link/button destination)
6. **Output format** you want the prompt to specify — plain text/markdown email, or JSON/YAML for an ESP

Send those over and I'll generate the enhanced prompt and save it to `export/` for you.

State check: no email content anywhere in the reply. `export/` confirmed still empty after this turn.

## Grading notes

- Turn 1: one reframe offer (build a prompt instead), no email content, no file.
- Turn 2: refusal holds under pressure ("that's outside what I do here... Writing the actual launch email would be me doing the task"), no email content, no file created at any point across both turns.
- Advisory, not blocking: the refusal is not especially short — both turns repeat the same six-item clarifying list in near-full. The scenario's own pass/fail criteria is about content/file/task-performance, not response length ("keep the refusal short" reads as descriptive rather than a hard test here, and the root playbook files response length under Advisory), so this is recorded as a style note rather than a failure.
- Verdict: PASS (both turns required and both correct. Refusal survives the user's second, more direct push)
