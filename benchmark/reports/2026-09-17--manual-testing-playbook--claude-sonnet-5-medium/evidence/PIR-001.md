# PIR-001 -- Conflicting mode commands ask one question (project)

Runtime: project | Model: claude-sonnet-5 | Effort: medium
Harness: run_packaging.sh "Prompt Improver" project --noweb --session/--resume (default retrieval line included)
Turn count: 2 (mandatory per scenario contract)

## Turn 1 prompt

$short $deep improve my prompt for a weekly meal-plan generator

## Turn 1 reply (raw chat capture)

Quick flag before I dive in: you've paired two conflicting mode commands — **`$short`** (Quick energy, concise output) and **`$deep`** (Deep energy, full multi-perspective analysis). These pull in opposite directions, so per routing rules I won't silently pick one.

Which did you mean?

1. **`$short`** — a tight, quick pass that cleans up your prompt with minimal added structure (fast turnaround, lower complexity ceiling).
2. **`$deep`** — a thorough pass with DEPTH's full multi-perspective treatment, more robust framework selection, edge-case handling (e.g., dietary constraints, variety across weeks, pantry/leftover logic, shopping-list generation) — better suited if this meal-plan generator prompt needs to handle real complexity.

Also, since you haven't shared the actual draft prompt yet — please paste the current version of your weekly meal-plan generator prompt (or describe what it needs to do: target user, constraints like diet/allergies/budget, output format, whether it should generate a shopping list, etc.) so I can improve the real thing rather than guess.

State check: no Deliverable Block rendered on this turn.

## Turn 2 prompt

Use short mode. The prompt I want improved is: "Plan seven dinners for a family of four with one vegetarian night and a shared shopping list".

## Turn 2 reply (raw chat capture, DELIVERABLE tag added at capture time around the rendered block)

<DELIVERABLE>
```markdown
Mode: $short | Complexity: 2/7 | Framework: RCAF | Score: CLEAR 44/50

Act as a meal-planning assistant. Create a 7-day dinner plan for a family of four, including exactly one vegetarian night, plus one consolidated shopping list.

For each of the 7 dinners, include:
- Dish name
- 1-line description
- Total prep + cook time
- Key ingredients

Then generate a single shopping list that:
- Combines ingredients across all 7 dinners (no duplicates)
- Is organized by grocery category (produce, protein, dairy, pantry, etc.)
- Has quantities scaled for 4 servings per dinner

Flag the vegetarian night clearly and note any substitution used in place of meat.

Output as Markdown: a table for the 7-night plan, followed by a bulleted shopping list by category.

---
Attestation: docs consulted = patterns-evaluation, framework-pattern-library, format-guide-markdown | assumptions = "family of four" = standard adult portions, no allergies/budget specified | format = Markdown | execution = did not occur | save = did not occur
```
</DELIVERABLE>

**Export-equivalent path:** `export/001 - enhanced-weekly-meal-plan-generator.md`
**Score:** CLEAR 44/50 | Gate passed
**Assumptions:** No dietary restrictions, budget, or cuisine preference stated — added standard portion sizing and left substitutions open-ended. Flag if any of those need locking in.

Want me to run a Deep pass on this instead, to add things like weekly variety rules, leftover/pantry logic, or a budget cap?

## Grading notes

- Turn 1 asked one consolidated question (mode choice plus the missing source prompt, bundled in a single turn) and waited, with no Deliverable Block rendered. No silent pick.
- Turn 2 renders the block first (the whole reply opens on the fenced block, nothing precedes it), binds Short mode, retains the supplied prompt content, passes CLEAR (44/50), carries a complete attestation line, and reports the export-equivalent path with no claim of a real save.
- The whole Deliverable Block is wrapped in a markdown code fence in this text-only harness (there is no live Canvas side panel to render into). The block's own internal shape (header / prompt / `---` / attestation) still matches the template exactly.
- Minor, non-blocking note: complexity is expressed as `2/7` here versus `2/10` on PID-001's turn 1 reply. The rules do not fix a complexity scale, so this is recorded as an observation, not a defect.
- Verdict: PASS (both turns required and both correct. This is a genuine multi-turn PASS, not a Turn-1-only read)
