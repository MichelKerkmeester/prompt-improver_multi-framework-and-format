# PFM-001 -- Independent $json format lock in the Project (project)

Runtime: project | Model: claude-sonnet-5 | Effort: medium
Harness: run_packaging.sh "Prompt Improver" project --noweb --session (single turn, Turn 1 delivered directly)
Turn count: 1 (Turn 1 delivered directly rather than asking a question, so the scenario's conditional Turn 2 was not triggered. The Turn 1 failure below is conclusive on its own)

## Turn 1 prompt

$improve $json Improve this and return it as JSON: "Summarize a meeting transcript into action items with owners and due dates".

## Turn 1 reply (raw chat capture, DELIVERABLE tag added at capture time around the rendered block)

<DELIVERABLE>
```json
{
  "role": "You are an expert meeting-notes analyst who converts raw meeting transcripts into precise, actionable task lists.",
  "context": "The user will provide a meeting transcript (plain text, may include speaker labels and timestamps). The summary will be shared with the team as the single source of truth for follow-up work, so every action item must be traceable to something actually said in the transcript.",
  "action": "Read the full transcript, identify every commitment, task, or follow-up mentioned, and extract: (1) a concise action item description, (2) the owner (the person who committed to or was assigned the task), and (3) the due date (explicit or inferred from relative references like 'by Friday'). If an owner or due date is not stated or reasonably inferable, mark that field as 'Not specified' rather than guessing.",
  "format": "Return a JSON array of objects, each with the keys \"action_item\", \"owner\", and \"due_date\" (ISO 8601 format, e.g. YYYY-MM-DD, or \"Not specified\"). Order items by due date, earliest first; items with no due date go last. Do not include any text outside the JSON array.",
  "input_placeholder": "{{MEETING_TRANSCRIPT}}"
}
```

---
Attestation: docs consulted = DEPTH Thinking Framework, Interactive Mode, Patterns and Evaluation, Framework Pattern Library, Format Guide JSON | assumptions = transcript is plain text with optional speaker labels; relative due dates should be resolved to ISO 8601 where inferable; unresolvable owner/due-date fields render as "Not specified" rather than blocking output | format = JSON | execution = did not occur | save = did not occur
</DELIVERABLE>

**Export-equivalent path:** `export/001 - enhanced-summarize-meeting-action-items.json`

**Score and gate status:** CLEAR 44/50 | Perspectives: 5 | Gate passed — Framework: RCAF (Role, Context, Action, Format), chosen over CO-STAR/TIDD-EC as the simplest fit for a single-output extraction task.

**Token overhead:** JSON adds roughly +7% tokens versus an equivalent Markdown version of this prompt (key quoting and structural braces).

**Summary:** The original one-liner lacked a role, handling rules for missing owners/dates, and an explicit output schema — all now specified. The RCAF structure keeps this lightweight while making the JSON output contract unambiguous for a downstream parser.

## Grading notes

- FAIL. No line beginning `Mode:` appears anywhere in the reply. Custom Instructions.md ALWAYS #17 requires the Deliverable Block to be "a single-line header plus enhanced prompt content and attestation footer only," and the DELIVERY PROTOCOL template is explicit that "the header line and the attestation line are delivery metadata that frame the payload" and "sit outside the JSON/YAML format lock." Here the model appears to have let the JSON format lock consume the entire block, including the position the header belongs in, so no `Mode: $improve | Complexity: ... | Framework: RCAF | Score: ...` line (or `Mode: $json | ...`, matching the skill twin's convention) was ever rendered. PFM-001's own expected signals name the header line explicitly ("the Canvas Artifact carries the header line, a prompt payload that parses as valid JSON and the attestation footer"), so this is a direct miss against that requirement, and it is exactly what Failure triage #1 for this scenario names: "Check the independent format axis... when the format or mode was lost."
- Everything else about this run is correct. The JSON payload (independently confirmed with `python3 -c "import json; json.load(...)"`) parses cleanly, the attestation line carries both required fields, token overhead is reported (+7%, inside the stated 5-10% band), the export-equivalent path is reported, and no save is claimed.
- No clarifying question was asked (enough was already supplied), so this scenario's conditional Turn 2 never triggers. The missing-header defect at Turn 1 is conclusive on its own.
- Twin note: the skill side (SFM-001) also fails this pair, but for the opposite defect. SFM-001 keeps the header line correctly and instead drops the required token-overhead report, while PFM-001 reports overhead correctly and drops the header line. Both packagings fail the same category for different reasons. This is not a twin disagreement (twin_divergence.py pairs verdicts, and both verdicts here are FAIL), but it is worth recording as two distinct defects rather than one shared one.
- Verdict: FAIL (missing required `Mode:` header line inside the Deliverable Block)
