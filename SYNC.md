---
title: "Prompt Improver - Parity Note"
description: "Hand-authored parity note for the Claude Project package. The AI System Sync Compiler is retired and Project files are written by hand from the skill sources."
---

# Prompt Improver Parity Note

The AI System Sync Compiler is retired. Nothing regenerates the Claude Project package anymore. The kernel and every knowledge file under `claude project/` are written by hand from the skill sources, following the manual parity method:

1. After a skill change, list `sk-prompt-improver/SKILL.md` against `claude project/Custom Instructions.md`, and each `sk-prompt-improver/references/*.md` or `sk-prompt-improver/assets/**/*.md` against its `claude project/knowledge/*.md` counterpart by name
2. Decide which paragraphs apply inside a Project: drop loading conditions, routed-by lines, file-path routing and benchmark references, keep the decision rules, output shapes and examples
3. Check the read-only drift list: a skill file whose last commit is newer than its Project counterpart means a review is due
4. Record each review as a dated sentence below or in the system README: who reviewed, what changed, what was decided

No manifest, lock file or checksum ledger is kept. The old `claude-project.sync.json`, `package-lock.json` and the generated regions were archived when the compiler retired.

- Bump the version in the `Custom Instructions.md` H1 in the same change that edits it. That number is what a Project reader sees, and a kernel whose rules moved while its version stood still is a kernel nobody can date. A rule change is a minor bump, a wording or typo fix is a patch.

## Review notes

- 2026-09-09 claude reviewed the knowledge files against the skill sources: all 13 were byte-identical compiler copies and none is a mirror of shared knowledge, so all 13 were hand-authored for the Project, dropping the frontmatter and the Loading Condition, Purpose and Scope blocks, renaming each H1 to its own Project title (the copies still carried skill-era titles such as Prompt - Templates - Visual Mode), and rewriting every cross-file skill path as the counterpart's Project title. Decided a Loading Condition bullet is never dropped whole where it also states a rule, so the command bindings ($vibe, $image, $video, and commands overriding the question flow) survive as plain rules, and every worked before-and-after pair carries over verbatim, verified by counting the 220 transformation arrows plus headings, bullets, checklist items, table rows and code fences before and after
- 2026-09-11 Michel Kerkmeester, with Claude Code, recorded the live kernel version and the change that reached this system without one. `Custom Instructions.md` is at v1.4.1, a patch bump from v1.4.0 made by the fleet-wide heading pass, which touched only headings, dividers and blank lines here, measured at zero non-heading lines across the whole system at that commit. `claude project/README.md` had told the operator to run `check --system prompt-improver --run-validators` before every upload, a command the retired sync compiler owned and which no longer exists, so an operator following it verified nothing while believing they had checked. It now names this system's own router fixtures. That same fleet commit also tried to correct the README's stale skill-version claim and made it worse, writing the kernel version into a field that names the skill, so the file asserted a skill version that does not exist. Corrected here by naming both numbers separately, kernel v1.4.1 and Skill v1.3.0, in the shape Deal Templates and Sales HubSpot already use, because the two version lines move independently and a single ambiguous number invites exactly this mistake. No knowledge file changed in this batch and this system holds no shared-knowledge mirror, so the kernel is the whole upload delta. `bash benchmark/router/run_fixtures.sh` passes 25 fixtures
- 2026-09-16 Michel Kerkmeester, with Claude Code, moved 2 kernel statements out of `Custom Instructions.md` into the documents that own them, leaving one pointer line in place of each. Every moved rule reached both sides of its pair, the Project knowledge mirror and the skill source it mirrors, because the gate does not compare rule content between them and a move that reached one side only would pass unseen. `Custom Instructions.md` is at v1.4.2, a patch bump from v1.4.1 for those cuts and the pointers that replaced them. Homes touched: Prompt Improver - Interactive Mode - v0.700, Prompt Improver - Patterns and Evaluation - v0.212
- 2026-09-16 Michel Kerkmeester, with Claude Code, moved 16 kernel statements out of `Custom Instructions.md` into the documents that own them, leaving one pointer line in place of each. Every moved rule reached both sides of its pair, the Project knowledge mirror and the skill source it mirrors, because the gate does not compare rule content between them and a move that reached one side only would pass unseen. `Custom Instructions.md` is at v1.4.3, a patch bump from v1.4.2 for those cuts and the pointers that replaced them. Homes touched: Prompt Improver - Interactive Mode - v0.700, Prompt Improver - DEPTH Thinking Framework - v0.200, Prompt Improver - Assets - Framework Pattern Library - v0.100, Prompt Improver - Patterns and Evaluation - v0.212
- 2026-09-16 Michel Kerkmeester, with Claude Code, moved 5 route statements out of `Custom Instructions.md` into the documents that own them, leaving one pointer line in place of each. Every moved rule reached both sides of its pair, the Project knowledge mirror and the skill source it mirrors, because the gate does not compare rule content between them and a move that reached one side only would pass unseen. `Custom Instructions.md` is at v1.4.4, a patch bump from v1.4.3 for those cuts and the pointers that replaced them. Two route rows stayed. One declares the phrase `the mode documents` as its home rather than a path. The other is the resource-domain list, whose declared span overlaps the span of the Consult the lane statement that must stay in the kernel, so cutting it would have taken the first three bullets that statement depends on. Homes touched: Prompt Improver - Patterns and Evaluation - v0.212, Prompt Improver - Format Guide Markdown - v0.141, Prompt Improver - Format Guide JSON - v0.142, Prompt Improver - Video Mode - v0.123, Prompt Improver - Image Mode - v0.123
- 2026-09-17 Michel Kerkmeester, with Claude Code, reconciled an inconsistent fleet rule about scoring context. One rule now applies across it: systems that score within one domain delete the imported sentence because its banned punctuation gives them no actionable guidance, while systems that score work from many domains keep the statement with the exact comma wording. The Prompt Improver mirror documents now carry `Scoring scales are calibrated per domain, so cross-project comparisons require context normalization.` byte identical on both sides because its prompts span many domains. The requested eight-system parity loop returned exit 2 for all eight because the validator refuses an uncommitted `systems.py`, so no declared pairs were checked. The declaration checks in `residency_check.py` and `validate_parity.py` cannot run against this uncommitted `systems.py`. The orchestrator must commit this worktree before running those checks
- 2026-09-18 Claude Code resolved a header contradiction inside the kernel, v1.4.5, Skill unchanged at v1.3.0. The delivery protocol's header template carried a fourth field, `Score:`, while ALWAYS 18 puts score, assumptions and docs consulted in chat after the block and the protocol's own preceding line says the Artifact carries the prompt and not the scoring explanation. Both format guides, the skill asset and its Project mirror, state the three-field form in six places each. So the four-field template was the outlier and it is removed rather than propagated. `benchmark/grader/deliverable_lint.py` required the fourth field too, citing ALWAYS 17, which says nothing about field count: that check flagged a correct three-field header and a header missing its Framework field identically, one violation each, so its finding carried no information. It now passes the correct header and still catches the malformed one. Separately the kernel specified the export-equivalent placeholder two ways, `[###]` at one line and `NNN` at another, against thirteen uses of `[###]` on the skill side, so `[###]` is now the only form here. Product Owner's own `NNN` convention is untouched. Two declared residency statements carried the old text and both are resynced with recomputed keys
- 2026-09-18 Claude Code made the no-filesystem constraint prescriptive per the operator's decision, kernel v1.4.6. The kernel already stated the fact as a reason for its delivery rule, which a runtime handed file tools can satisfy while skipping the instruction: three runs during the fleet test wrote files outside every scratch tree, one of them attesting that no save occurred in the same reply. The delivery line now binds whatever tools appear available, so never hand back a path or a save confirmation in place of the rendered Artifact. Skill unchanged, because no skill rule changed and the skill packaging legitimately writes files
- 2026-09-18 Correction to the entry above and to commit 076133a, both of which say the `[###]` placeholder had thirteen uses on the skill side. It has twelve across the skill tree and three in `SKILL.md` itself. The thirteenth was the kernel's own use at line 291, which is the document being corrected rather than evidence about the skill side, so counting it made the case for `[###]` look one stronger than it was. The decision does not change, twelve against one is the same conclusion, and the number is wrong and is corrected here because a commit message cannot be. Found by an independent verdict lane that tried three scopes and could not reach thirteen by any of them
- 2026-09-20 kernel v1.4.7 names every Knowledge document it delegates to by its Project filename plus the topic that document carries, replacing the title-only pointer text; no pointer line repeats inside one rule block. The numbered list items that earlier moves had left out of sequence now run 1..n again. No rule text, no Knowledge document and no Skill change; Skill stays v1.3.0.
- 2026-09-24 Claude Code, at Michel Kerkmeester's request, reverted 26814450 in e073d659. That commit was a formatting-only edit, the resource table re-padded, that left the kernel heading at v1.4.7, so the version no longer named one text. The revert puts the kernel back byte for byte, and v1.4.7 again names the text a live Project runs. The parity gate reports e073d659 as a kernel commit that did not move the H1, because it cannot tell a revert from an edit. That finding is expected. This is a maintenance record, not a review.
- 2026-09-25 Claude Code, at Michel Kerkmeester's request, taught the parity gate to tell a restore from an edit in 8922b1dd. A kernel commit that leaves the H1 alone and puts back the exact text its version was introduced with now counts as a restore, so the gate records e073d659 as restoring the text 0938e2c3 introduced v1.4.7 with, and 26814450 as a stall that revert repaired. The finding the entry above calls expected no longer appears. This is a maintenance record, not a review.
- 2026-09-25 Claude Code, at Michel Kerkmeester's request, carried three operator decisions from the run in `benchmark/reports/2026-09-25--manual-testing-playbook--claude-sonnet-5-medium` into kernel v1.5.0, a minor bump from v1.4.7 because rules moved. First, an unrequested extra output is scope expansion even when the reply flags it. `SKILL.md` now says in its Objective that a default fills a gap in what the user asked for, never adds an output, field or section the user did not ask for, and is not made allowed by a flag, and its NEVER 4 and Scope blocker lines say the same. The kernel ban named no flag case while the Knowledge it delegates to carries the same flagged-defaults fallback and assumption rule the skill had, and a Project run in that report added a flagged field under it, so the kernel Objective gained the same two sentences, placed after the declared Objective statement so no residency key moved. No Knowledge file changed, because its fallback and assumption lines match `SKILL.md` lines left as written and the new definition now governs them on both sides. Second, a revision the user asks for after a delivery is saved as a new export under the next number, and a delivered export is never edited in place. That rule is now in `SKILL.md` Export-First Delivery and in the `AGENTS.md` export protocol, because a skill run in that report edited its first export in place. The Project writes no files and reports a `[###]` placeholder, and nothing on its side contradicts the rule, so it did not change. Third, the kernel Delivery Protocol gained a no-Canvas rule: a session with no Canvas panel renders the Deliverable Block as one fenced block at the very start of the reply, with no preamble about the missing panel or the mode detection, then the chat items as usual, while the claude.ai Canvas rule stands unchanged. A Project run in that report failed on exactly that preamble. The skill needs no counterpart, since its Claude Projects Delivery Override already puts one fenced Deliverable Block first. The Project README kernel claims now read v1.5.0. `SKILL.md` moves from 1.3.0 to 1.4.0, a minor bump because its rules moved, with its changelog entry, and the Project README skill claims follow. The kernel review by Michel Kerkmeester is still pending. This is a maintenance record, not a review.
